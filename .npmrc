# Ensure optional dependencies are installed
# This is critical for platform-specific binaries (Tailwind CSS oxide, lightningcss)
install-optional=true

# Don't skip optional dependencies
no-optional=false
